package it.polimi.ingsw.gui_test;

import it.polimi.ingsw.view.game_elements.VDice;
import it.polimi.ingsw.view.other_elements.VColor;
import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.layout.GridPane;
import javafx.stage.Stage;
import org.junit.jupiter.api.Test;

import static javafx.geometry.Pos.TOP_LEFT;

/*
public class VDiceTest extends Application {
    public void VDiceTest() {
        launch();
    }

    @Override
    public void start(Stage primaryStage) {
        VDice dice = new VDice(5, VColor.PURPLE);
        GridPane root = dice.toGUI();
        Scene scene = new Scene(root);
        primaryStage.setTitle("Dice");
        primaryStage.setScene(scene);
        primaryStage.show();
    }
}
*/